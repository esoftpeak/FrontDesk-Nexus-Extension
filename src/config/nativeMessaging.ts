/**
 * Native Messaging (Windows) — documentation only.
 *
 * Host manifest `name` and registry must match {@link NATIVE_HOST_NAME} in `src/shared/protocol.ts`
 * (currently `com.frontdesk.nexus`).
 *
 * Example `allowed_origins` entry — replace with your extension ID from chrome://extensions:
 * `chrome-extension://dhhlencfcfageiedbagdomapcfgbnhmf/`
 *
 * Python entry: `python.exe path/to/main.py --native-messaging`
 */

export {}
